namespace AutoConfig.Core.Enums;

public enum Role { User, Admin }
public enum FuelType { Petrol, Diesel, Electric, Hybrid }
public enum OptionCategory { Color, Interior, Technology, Safety, Comfort }
public enum CarCategory { Sedan, Suv, Coupe, Hatchback, Wagon }
public enum QuoteStatus { Pending, Approved, Rejected, Expired }
